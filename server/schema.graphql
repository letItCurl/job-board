# @NOTE: Job! no null element in the list
type Query {
  jobs: [Job!]
}

# @NOTE: ID! means not null
type Job {
  id: ID!
  title: String!
  description: String
  company: Company!
}

type Company {
  id: ID!
  name: String!
}
